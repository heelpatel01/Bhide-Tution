<nav class="navbar navbar-expand-lg navbar-dark bg-body-tertiary bg-dark" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Bhide Tution Class</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
      aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation" style="color: white;">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Home</a>
        </li>
        <% if (locals.user && user.role==="ADMIN" ) { %>
          <li class="nav-item">
            <a class="nav-link" href="/course/add-new">Add Course</a>
          </li>
          <% } %>
            <li class="nav-item">
              <a class="nav-link" href="/courses">See Courses</a>
            </li>
            <% if (locals.user) { %>
              <ul class="nav-item">
                <a href="/user/yourLearnings/<%= user._id %>" class="nav-link">My Courses</a>
              </ul>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                  aria-expanded="false">
                  <%= JSON.stringify(locals.user.email) %>
                </a>
                <ul class="dropdown-menu">
                  <li><a class="dropdown-item" href="/user/logout">Logout</a></li>
                </ul>

              </li>

              <% } else { %>
                <li class="nav-item">
                  <a class="nav-link" href="/user/signin">Signin</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="/user/signup">Create Account</a>
                </li>
                <% } %>
      </ul>
    </div>
  </div>
</nav>

<% if (locals.error) { %>
  <div class="alert alert-danger mt-3">
    Error occurred: <%= locals.error %>
  </div>
  <% } %>